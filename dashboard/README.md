# Dashboard

Aplicacao inicial em Streamlit para monitoramento dos custos da dialise no SUS.

## Como rodar

Na raiz do projeto, execute:

```bash
streamlit run dashboard/app.py
```

O dashboard usa os arquivos tratados em `dados_tratados/`.
